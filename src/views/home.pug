doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Hi There🖐 (Video Chat)
        link(rel="preconnect", href="https://fonts.googleapis.com")
        link(rel="preconnect", href="https://fonts.gstatic.com" crossorigin)
        link(rel="stylesheet", href="https://fonts.googleapis.com/css2?family=Noto+Serif+KR&display=swap")

    body 
        header
            h1 Hi There🖐 (Video Chat Service)
            h3 채팅방 이름과 닉네임을 입력해주세요.
        main
            div#welcome
                form 
                    input#roomName(placeholder="채팅방 이름을 입력하세요.", required, type="text")
                    input#nickName(placeholder="사용할 닉네임을 입력하세요.", required, type="text")
                    button 입장하기
            div#call
                div#myStream
                    video#myFace(autoplay, playsinline, width="400", height="400")
                    button#mute Mute
                    button#camera Turn Off Camera
                    select#cameras
                    video#peerFace(autoplay, playsinline, width="400", height="400")
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app.js")
        style 
            include style.css
            